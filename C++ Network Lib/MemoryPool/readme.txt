메모리 풀로 반환시 무조건 객체의 소멸자가 호출되게끔 하였다.

따라서 동적 할당 포인터 하나를 들고있는 식의 객체에 대해서는 메모리풀 사용의 효과가 거의 없을것이지만 사용에 일관성을 가져다주는 이점이 있다고 판단되어 이렇게 정책을 선택했다.
-> 이거때문에 직렬화 버퍼도 동적 할당 포인터가 아닌 객체 자체가 공간을 가지는 형태로 변경했음.

중요!!!
그리고 오브젝트 Get, Return시 멀티쓰레드에서도 안전하게 변경해야겠다. <- 3분기쯤..

중요!!!
메모리 풀을 무조건 싱글턴 타입으로 사용해야 한다. 동일 객체 타입의 메모리풀을 여러군데에서 생성할 시 
생성자에서 기존 Heap Code를 덮어써버리기 때문에...
그리고 검증 코드도 성능을 위해 static으로 해놨기 때문에 싱글톤 타입으로만 써야한다.

그러나 성능을 포기하면 동일 객체 타입에 대해서 얼마든지 여러개의 메모리 풀 인스턴스를 가지게 설계를 바꿀 수 있다.